export type LocalTrackedResource = {
    currentCount: number,
    isTracked: boolean
}

export type CatalystCost = {
    id: number,
    name: string,
    code: string,
    count: number,
    isEpic: boolean
}